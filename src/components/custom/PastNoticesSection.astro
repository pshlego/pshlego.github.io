---
import type { Post } from "@/lib/interfaces";
import UpdateCard from "./UpdateCard.astro";

interface Props {
    posts: Post[];
    title?: string;
}

const { posts, title = "Past notices" } = Astro.props;
---

<section class="past-notices-section max-w-[850px] sm:mr-20 print:max-w-full print:mr-auto">
    <h2 id="auto-past-notices" class="text-2xl font-semibold mb-6 hasId">{title}</h2>
    <div class="past-notices-list">
        {posts.map(post => (
            <UpdateCard post={post} />
        ))}
    </div>
</section>
